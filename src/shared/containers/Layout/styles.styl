@import "../../styles/common";

.Layout {
	& {

	}

	&__wrapper {
		display: flex;
		flex-direction: column;
		min-height: 100vh;
		overflow hidden
	}

	&__header {

	}

	&__content {
		flex: 1;
		padding 0 $paddingX
	}

	&__footer {

	}
}