@import '../../styles/common.styl'

.PageInfo {
	background #CCC
	color #000
	&__container {
		container()
		padding 15px 0
		display flex
		justify-content space-between
		align-items center
	}
	&__name {
		font-size 24px
	}
}