import React from 'react';
import style from './styles.styl';

const ScrollIcon = () => {
	return(
		<div className={style.ScrollIcon}>
			<span className={style.ScrollIcon__weel} />
		</div>
	)
}

export default ScrollIcon